import React from "react";
import styled from "styled-components";

export const Footer = () => {
    return (
        <Wrapper> 
            FOOTER 
        </Wrapper>
    );
  };
  
const Wrapper = styled.div`
    display: flex;
    background: blue;

    height: 3vh;
`;