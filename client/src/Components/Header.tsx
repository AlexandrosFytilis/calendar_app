import React from "react";
import styled from "styled-components";

export const Header = () => {
    return (
        <Wrapper> 
            HEADER 
        </Wrapper>
    );
  };
  
  const Wrapper = styled.div`
  display: flex;
  background: blue;

  height: 7vh;
`;